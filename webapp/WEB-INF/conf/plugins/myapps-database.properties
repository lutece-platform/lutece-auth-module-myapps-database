################################################################################
# Configuration file for Myapps database module

# pagination options
myapps-database.itemsPerPage=50

# Provider default Name
myapps-database.provider.defaultName=Registred applications

# Default field size. It should always be < or = to 255 (value of the attribute in the database)
myapps-database.field.size=255
# The key is used to encode the login and the password of a user for make connection on his applications
crypto.key=